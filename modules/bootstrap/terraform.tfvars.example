bucket_name         = "my-company-terraform-state-abc123"   # must be globally unique
dynamodb_table_name = "terraform-state-locks"
aws_region          = "us-east-1"

tags = {
  Project   = "myapp"
  ManagedBy = "Terraform"
}
